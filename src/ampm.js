//Write a function which can convert the
// time input given in 12 hours format to 24 hours format

let time = "12:10PM";

function ampm(time) {
  let h = time.split(":");
  console.log(h);
  return;
}
ampm(time);
// console.log(`am pm time is${ampm(time)}`)
